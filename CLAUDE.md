# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this project is

**SciSpark Paper Manager** — an AI-agent-driven wiki / knowledge-base system with rich visualization, for researchers to track the fields they care about. It generalizes SciSpark (currently a clinical-evidence product) to all research domains and adds a persistent personal knowledge layer on top of SciSpark's recommendation feed.

**Status: implementation in progress — M1 (vault core), M2 (LLM harness), M3 (proxy backend), M4 (ingest + wiki UI), M5 (feed + user model), M6 (reader + highlights + select-to-ask), M7 (Research Companion), M8 (visualization dashboard), M9 (Spark skill), M10 (trending dashboard), and M11 (local-runtime pivot), and M12 (lint + spend panel + hardening) are all merged to main.** The core product loop is live-verified end-to-end (2026-07-12, GMI Cloud `anthropic/claude-sonnet-5`): /papers search → digest → Add to Knowledge Base → validated atomic wiki changesets → review inbox → one-click undo, ~$0.29/paper. M5 adds the second live loop (2026-07-13, same model): onboarding seeds the two-tier user model (`profile.md`/`interests.md`/`feedback.md` + Tier-1 `.scispark/events/` log) → Agentic Research Feed funnel (strong strategy → fast rank → strong re-rank with "why this/you/now") → personalized home, ~$0.21–0.24/refresh; a Memory-Consolidation skill distills events back into the editable user-model pages via changesets. M6 adds the in-app reader at `/reader?paperKey=` (arXiv/PMC HTML via the `/api/fetch` relay, DOMPurify-sanitized with inert links + `[figure]` placeholders; PDF via pdf.js), quote+context+position-anchored persistent highlights (`highlights/<key>.json`), and select-to-ask (Reading-Companion `strong` skill, live-verified $0.0101/ask; Highlight; Capture-idea → `note` changeset), with highlights feeding ingest emphasis. LLM access is BYOK via settings `baseUrls` override (see M2 plan status block). Known GMI-infra flakiness: its Anthropic passthrough intermittently 400s structured-output calls (`output_config.format`) via backend-replica variance — **hardened (M9): `OpenAICompatProvider` now falls back once to prompt-embedded JSON on exactly that rejection (`isStructuredOutputRejection`), with `safeParse` tolerating fenced/prose JSON; zod re-validation stays the enforcement layer. `searchArxiv` also retries 429/5xx + network errors with backoff (honoring `Retry-After`), and the Deep scoop-check caps source-search concurrency at 4 via a shared limiter — together these made the Deep live gate pass cleanly.** Reader HTML now has scoped prose typography on `.reader-surface` (serif Halant headings, spacing, styled lists/tables/code, dashed `[figure]` placeholder) — browser-verified against a live arXiv paper. M7 adds the Research Companion **Ember** (user-renamable via settings): a persistent mascot + dismissible speech bubbles, a deterministic LLM-free proactivity engine (app-open / post-ingest / review-pending / sparkable-cluster triggers), a `fast`-tier in-persona utterance skill (live-verified $0.0029/utterance) with template fallback, and a harness-enforced anti-Clippy contract (chattiness session budget where off ⇒ zero LLM, per-trigger cooldowns, dismissals/actions logged as Tier-1 events); the persona tone-wraps Reading-Companion answers (grounding rules untouched). **Manual-check follow-ups: M6 — hand-drive select→highlight→ask + PDF surface (plus MathML/KaTeX + arXiv page-chrome trimming as reader polish); M7 — drive a live trigger→bubble→dismiss/action flow visually.** M8 adds the `/viz` dashboard: four views derived live from the wiki (nothing stored) — knowledge graph (Sigma.js + graphology, 4-signal weights + Louvain, WebGL, `ssr:false`), field timeline, citation flow (via a new `/api/citations` Semantic Scholar proxy + `.scispark/citations/` cache), and author co-authorship network (d3-force). Zero-LLM milestone (browser-verified all four tabs, no live gate). A repo-wide source-hygiene test now bans raw control bytes in source (a literal-NUL-separator bug had made three files binary-undiffable mid-milestone). M9 adds **Spark** — vault-grounded research-idea generation adapting the *discipline* of microsoft/ResearchStudio (MIT; 46 pattern cards bundled with attribution) at `/spark`. Two modes: **Quick Spark** (`src/lib/spark/quick.ts`, vault-only single `strong` call → 1–3 idea seeds, cents; each seed Save→stub `idea` page or Develop-fully→Deep) and **Deep Spark** (`src/lib/spark/deep.ts`, user-confirmed ~$1–3 pipeline: grounding → bottleneck-diagnosis → pattern-guided ideation → two-channel scoop-check → 5-check corpus-anchored audit → assemble → atomic `idea`-page changeset). Core disciplines: a **falsification lock** (4 mandatory non-empty fields the audit may rewrite but never drop), honest `do_not_generate`/`abandoned` exits (never a forced idea), one internal ideation→scoop→audit retry, and every phase an isolated storage-free `runSkill` (orchestrators own storage). UI: `/spark` (Quick seeds + Deep confirm→live-phase-progress→card), an idea gallery over `wiki/ideas/*`, `status`/`depth` badges in the wiki view, and the companion's sparkable-cluster trigger linking through. Deep runs are serialized (no double-spend) and an upfront `estimateDeepSparkCost()` (~$1.10) gates the confirm dialog. Env-gated live gate `live-spark.test.ts` (`LIVE_LLM_*` vars). **Live-run status (2026-07-14, GMI `anthropic/claude-sonnet-5`): BOTH modes live-verified green after the infra hardening above. Quick Spark: 3 seeds, $0.016. Deep Spark: outcome `idea` (page written + parseable, status `sparked`, all 4 falsification fields + scoop verdict), $0.264 total across 5 phases (sample idea: "Sketch-and-Verify KV Cache: Provable Recall Bounds via Johnson–Lindenstrauss Compression Instead of Eviction"). A first pass had failed on arXiv 429/503 + the GMI `output_config` flake + a razor-thin 300s cap; the provider prompt-JSON fallback + arXiv retry/backoff + scoop concurrency cap fixed it, and the gate's Deep timeout was widened to 7min for margin.** M10 (trending dashboard) is merged: **live-verified 2026-07-14 vs GMI ($0.041/field — real deterministic metrics + schema-valid survey, no flakes), 1046 tests green; next per roadmap: M11 (lint + hardening).** M10 was reframed by Tong (2026-07-14) from a public/anonymous trending page + server Vercel cron/blob + marketing account capture to a **personalized field-trends dashboard** at `/trending`: 1–3 fixed sub-fields seeded from `interests.md` (editable on `/profile`), per-field deterministic metrics + charts (`src/lib/trending/*`: retrieval, `computeFieldMetrics`, publication-volume/momentum/top-movers views) plus a persona-free `strong`-tier survey skill (`src/lib/skills/trending.ts`, qualitative-only — notable-paper whys, emerging topics, momentum narrative; no LLM-emitted numbers). **v1.1 (2026-07-15) shipped real per-week aggregation**: `weeklyVolume` is now real OpenAlex per-week work counts, and `pctChange` is **week-aligned** (last 2 weekly buckets vs the 2 before, `null` when the prior sum is 0). Mechanism: one **`group_by=publication_date` request per field** (1 OpenAlex credit, `groupWorksByPublicationDate` + `nodeGroupFn`) with a fallback ladder — grouped → per-week `countOpenAlexWorks` searches → the old retrieval-sample series (never a dropped panel). **OpenAlex is now credit-priced** (keyless $0.10/day ≈ 100 searches; a **free API key** = `OPENALEX_API_KEY` env, `api_key` param → $1/day; resets midnight UTC; details in `docs/design/03-backend.md`) — per-user local runtime keeps real usage well inside even the keyless tier, and `searchOpenAlex`/counts now retry 429/5xx with backoff. **Live-verified 2026-07-15** (GMI + OpenAlex free key): real multi-week series `[7504,…,5224,204(partial current week)]` for NLP via the 1-credit path, schema-valid survey, $0.0436. Known display caveat: the newest bucket is the in-progress week (systematically low) — a UI tooltip is a queued polish item. Scheduling is a **staleness-triggered refresh** standing in for a v1 "cron" (cache at `.scispark/trending/dashboard.json` with `generatedAt`, cadence in `.scispark/settings.json`, manual Refresh always available). The original public/cron/Vercel-Blob/`/api/register`/Supabase model is dropped for v1 and kept as a documented **v2** growth path (see `docs/design/03-backend.md` and `docs/superpowers/specs/2026-07-14-m10-trending-dashboard-design.md`). **M11 (local-runtime pivot) is merged — live-verified end-to-end 2026-07-14 vs GMI (real local server, vault scaffolded on disk, key hygiene enforced, trending skill executed server-side, $0.0401; 1177 tests green). Next per roadmap: M12 (Lint Skill, spend panel, export/import QA — the old M11 scope — plus deferred hardening).** Tong's directive (2026-07-14): "the browser is only for showing UI; runtime is on user." The browser is now **UI-only** over a local Next.js server that owns the whole runtime: `NodeFsVaultStorage` puts the vault on disk via `node:fs` at `SCISPARK_VAULT` (default `~/SciSpark/vault`), replacing OPFS (**removed** — `OpfsVaultStorage` is no longer in the user path); a vault API (`/api/vault/file`, `/api/vault/list`, `/api/vault/changeset`) backs a client-side `RemoteVaultStorage` proxying over `fetch`, so every existing page/component keeps working unchanged against the same `VaultStorage` interface, with changeset apply/revert running server-side for atomicity; a skills API (`/api/skills/*`) runs every orchestrator (feed refresh, digest, ingest, ask, chat, spark quick/deep/estimate, trending refresh, companion utterance, consolidate) server-side unchanged, streaming progress via SSE for long runs; and `/api/settings` holds BYOK keys server-side (`GET` redacts key values to presence flags, `PUT` accepts updates, the browser never receives stored key material back) — the vault's own `.scispark/settings.json` is deliberately **unreachable through the generic vault-file route** (`/api/vault/file` 403s on that path; keys are managed exclusively via `/api/settings`). A repo-wide browser-purity test enforces the UI-only boundary (no server-only imports reaching client bundles). **M12 follow-up (2026-07-15): that 403 lockdown had orphaned every OTHER client settings surface — companion name/chattiness and trending fields/cadence still read/wrote settings.json via the browser's `RemoteVaultStorage` and hit the 403, so `/debug/llm`, `/trending`, and `/profile` couldn't persist them. Fixed by making `/api/settings` own the WHOLE file: GET returns the `companion`+`trending` sub-objects in full (only `llm.keys` redacted), PUT accepts `{patch?, companion?, trending?}` (each touched only when present, one `withSettingsWrite` section), validated by shared pure `normalize{Companion,Trending}Settings`; new `{companion,trending}/settings-client.ts` wrappers (mirror `llm/settings-client.ts`) replace the direct `RemoteVaultStorage` calls in those pages + `ReaderView`, and the browser-purity gate now bans `load/save{Companion,Trending}Settings` from client code. Ride-along: `RemoteVaultStorage` stored the bare global `fetch` and invoked it as `this.fetchFn(...)` — a method call whose receiver is the instance — so native `fetch` threw "Illegal invocation" and bricked ALL browser vault reads (`/wiki` etc.); it now calls through a plain-call wrapper (unit tests had masked this with a receiver-agnostic mock; the browser paths had never been hand-driven). Browser-verified end-to-end against a real on-disk vault.** The hosted/Vercel deployment remains a documented future paid tier, not the v1 runtime; Tauri is a post-v1 wrapper on this same code. See `docs/superpowers/specs/2026-07-14-m11-local-runtime-design.md`. The old M11 scope (Lint Skill, spend panel, export/import QA) moved to **M12 (now merged)**. **M12 (2026-07-14) ships: the Lint Skill** — free deterministic checks (orphans, broken wikilinks, bad frontmatter, index drift — pure over the bundle) + a manual `fast`-screen→`strong`-judge LLM pass (contradictions/stale claims, persona-free) → findings land in the review inbox as a new `lint-finding` kind with one-click **undoable fix changesets recomputed at apply time** (so sibling fixes never conflict; index-drift special-cased via `writeIndex` since `index.md` is a protected path); runs server-side via `/api/skills/lint{,/estimate,/fix}`; UI = a "Lint vault" control + deep-lint cost-confirm on `/wiki/inbox`. Live-verified vs GMI ($0.0127, planted contradiction caught). **A spend panel** — `GET /api/usage` aggregates `.scispark/usage/*.jsonl` (`summarizeUsage`, pure), rendered on `/debug/llm` (today-vs-budget bar, 7-day chart, per-skill table, editable `dailyBudgetUsd`). **Hardening**: one shared per-storage settings write-lock (`src/lib/vault/settings-write.ts`, closes the `llm`-had-no-queue lost-update race; `/api/settings` PUT routes through it), `AbortController` fetch timeouts on arXiv/OpenAlex, and GMI prompt-JSON-fallback telemetry (counter surfaced on the debug ping route). Also: export→import round-trip tests, and a real latent fix in shared `frontmatter.ts` (`parseDocument` stripped a stray leading `\n` so `serialize∘parse` is now a true inverse — the prior drift silently broke `applyChangeset` conflict checks on recomputed content). Deferred to a future pass: dead-code cleanup (mock-feed subtree, unused `browserSearchFn`), a localhost auth token, and relocating the spend panel to a user-facing settings surface. (The v1.1 trending per-week-aggregation retrieval improvement, once deferred here, has since shipped — see the M10 entry above.) **This completes the v1 roadmap (M1–M12 all merged).** **Post-v1 followup (2026-07-15): arXiv search relevance + intent-driven ranking.** `/papers` arXiv search was returning off-topic newest papers for topical multi-word queries (loose `all:<phrase>` + unconditional `submittedDate` sort). Fixed in two coupled parts: (1) the arXiv adapter now **AND-joins** plain free-text terms for precision but passes **structured** queries (arXiv field prefixes / boolean `AND`/`OR`/`ANDNOT`, which the Feed strategy prompt deliberately emits) through **verbatim** so they aren't mangled; (2) per Tong's directive, relevance-vs-recency ranking is **extracted as a step before the search, not hardcoded in the adapter** — a new persona-free `fast`-tier **Search-Intent Skill** (`src/lib/skills/search-intent.ts`, `POST /api/skills/search-intent`, memoized client wrapper, degrades to `relevance` on any failure) classifies the typed query, and the chosen `sort` threads through `/api/search?sort=` → `ArxivQuery`/`OpenAlexQuery.sort` (an optional override — omitted keeps each adapter's historical default: arXiv newest-first, OpenAlex relevance, so all server-side `nodeSearchFn` callers stay unregressed). Browser-verified (relevance results for the EEG query); arXiv relevance live gate green; `LIVE_LLM_*` intent-classifier live gate added (`live-search-intent.test.ts`). See `docs/design/03-backend.md` ("Search ranking = extracted intent") and `04-agent-harness.md` (Search-Intent row). Ride-along fix lists live in each plan's status block and the SDD ledger. **Post-v1 followup (2026-07-15): trending survey failure-honesty + LLM-call timeout.** A live `/trending` run against GMI surfaced two *silent-failure* issues (the survey itself works — the null surveys were the documented GMI structured-output flake, which recovers on retry; the real vault's later refresh came back with all panels populated). (1) A failed qualitative survey degraded the panel to `survey: null` but swallowed the reason: `FieldPanelView` ignored the panel error and `completeStructured` discarded a failed structured call's spent tokens (so it never hit `.scispark/usage` — "$0 in the log but charged at GMI"). Fixed: `FieldPanel.error` → **`surveyError`** (rendered on the panel as a "Reason:" line), and `StructuredOutputError` now carries `usage` which the runner **meters on failure** — failures are billed honestly. (2) `OpenAICompatProvider` used raw `fetch` with **no timeout** (the arXiv/OpenAlex paths already had `AbortController` timeouts) — a hung GMI response blocked the *sequential* trending field loop before its persist step, so a manual Refresh could bill surveys yet never rewrite `dashboard.json`. Fixed: a **120s abort timeout** (`DEFAULT_LLM_TIMEOUT_MS`, covers connection/headers/body) → a hang degrades the field and the dashboard still persists. Also bumped the survey `maxTokens` 2048→4096 (matching feed/digest) so a reasoning-heavy completion can't truncate the JSON. The refresh write path itself was proven correct (tests: two refreshes advance `generatedAt` on disk; a degraded refresh still persists). Vitest now runs `.test.tsx` under `src/components/**` (a `react-dom/server` render test guards the surveyError line). 1369 tests green. **Post-v1 followup (2026-07-16): reader interaction fixes (Tong's first live hand-drive of the M6 select→highlight→ask loop — that manual-check item is now done; PDF surface still pending).** Three user-visible reader bugs fixed, all live-verified against a real vault: (1) **persisted highlights didn't re-paint after reload** — `HighlightLayer` measured the DOM during render (`useMemo`) and only recomputed on window resize/scroll, so rects computed before layout settled (zero-size pane, font swap, figure loads) stayed empty/stale forever; painting now runs in effects and re-fires on a `ResizeObserver` on the surface + `document.fonts.ready` + window events, and the mount-time highlights load retries transient transport failures (`listHighlightsWithRetry`) instead of silently leaving the session highlight-less. (2) **Text selection died mid-drag** ("selects everything before the cursor, gone on release") — TRUE root cause, found via an in-page event recorder: **React 19.2 re-applies `dangerouslySetInnerHTML` — destroying and recreating every child node — whenever the wrapper OBJECT identity changes, even with an identical `__html` string** (verified via fiber props; ≤React 18 value-compared, so the inline `{{__html}}` literal idiom silently became a DOM-nuker). Every selectionchange-driven re-render rebuilt the surface's DOM, clamping the in-progress selection to the container start. Fix: memoized wrapper in `HtmlSurface`; red/green regression test asserts DOM-node identity survives parent re-renders. **Any future `dangerouslySetInnerHTML` in this repo must pass a referentially stable wrapper.** Ride-along: painted highlight rects are now `pointer-events-none` (they were intercepting selection drags over highlighted text; click-to-remove moved to a collapsed-selection-guarded hit-test so drag-end can never delete a highlight). (3) **Capture-idea threw in embedded webviews** — `window.prompt` isn't implemented there (and would break the post-v1 Tauri wrapper); replaced with an inline `CaptureIdeaCard` (optional-thought textarea, Esc/Cancel, save errors surfaced on the card instead of swallowed). 1387 tests green. Reader-rendering triage from the same session: bracketed citations like `[cherry1953some]` are the paper's own arXiv LaTeXML HTML (`ltx_missing_citation` — unresolved bib in the source; not ours to fix), "empty tables" are LaTeXML *equation groups* whose MathML the sanitizer strips (fix approved: allow DOMPurify's vetted `mathMl` profile through — browsers render MathML Core natively), and missing figures are the documented M6 `[figure]` placeholder design (real figure rendering approved as next work). The authoritative design lives in `docs/design/01-product.md` through `05-frontend.md` (product → system → backend → agent harness → frontend, per Tong's five-layer split); `docs/design/06-roadmap.md` is the 11-milestone build order; implementation plans live in `docs/superpowers/plans/` (one per milestone). Read the design docs before this file's decision log when they conflict.

## Running, testing & user-testing (practical)

**Run the app (M11 local-runtime model — the Next.js server IS the runtime; any browser is UI-only):**
- `npm run dev` → http://localhost:3000 (or `npm run build && npm run start`). There is a `.claude/launch.json` for the preview tools.
- **Vault** = plain files on disk at `SCISPARK_VAULT` (default `~/SciSpark/vault`), auto-scaffolded on first server start. Delete/point elsewhere for a fresh-user simulation.
- **`.env.local`** (gitignored; values live only on this machine): `OPENALEX_MAILTO`, `OPENALEX_API_KEY` (free key = $1/day OpenAlex quota; keyless $0.10/day, resets midnight UTC).
- **LLM key (BYOK)**: enter on `/settings` (the user-facing **Connect your AI** card — provider presets Anthropic/OpenAI/OpenRouter/Google plus an **Other** tab that takes a Server URL + Model for any OpenAI-compatible endpoint, masked key field, Save & test connection; `/debug/llm` remains a dev harness) → stored server-side in the vault's `.scispark/settings.json` via `/api/settings` (GET redacts values; the vault-file API 403s that path). Tiers map to models there too. Without a key, LLM surfaces show the missing-key affordance; deterministic surfaces (viz, deterministic lint, cached views) still work.

**Commands:** `npm test` / `npx vitest run` (full suite; ~1310 tests green as of v1.1), `npx tsc --noEmit`, `npm run lint` (baseline: 8 errors/6 warnings, ALL pre-existing in fork-era mock files — new code must add none), `npm run build`. Env-gated live gates (real money, never CI): `LIVE_LLM_BASE_URL/LIVE_LLM_MODEL/LIVE_LLM_API_KEY npx vitest run src/lib/{skills,spark,trending,lint}/__tests__/live-*.test.ts`.

**Real vs fork-mock surfaces** (a user test should exercise the REAL ones; the mock ones are fork leftovers kept as placeholders):
- **Real (M1–M12 + v1.1):** `/` (personalized feed; onboarding gate), `/onboarding`, `/papers` (search → digest → Add to Knowledge Base → undo), `/reader?paperKey=` (HTML/PDF, highlights, select-to-ask, capture-idea), `/wiki`, `/wiki/[...id]`, `/wiki/inbox` (review queue + Lint vault + deep-lint), `/viz` (4 views), `/spark` (Quick/Deep), `/trending`, `/profile` (trending fields + cadence), `/settings` (BYOK Connect-your-AI card with provider presets + Other/OpenAI-compatible tab, AI-spend/budget panel, companion name + chattiness), `/debug/llm` (dev harness: raw keys/tiers + test-completion buttons), `/debug/papers`, `/debug/vault`. The companion (Ember) overlays all pages.
- **Fork-mock (NOT real yet — don't user-test as product):** `/chat`, `/chat/[id]` (KB chat is still mock!), `/history`, `/library`, `/projects`, `/paper/[id]`.

**User-test starting points:** the core loop = onboarding → feed refresh → open a paper on `/papers` → digest → Add to Knowledge Base → `/wiki/inbox` review/undo → `/reader` highlight + ask → `/spark` Quick → `/trending`. Accumulated **browser-manual checklists** (never yet hand-driven: reader select→highlight→ask + PDF, companion trigger→bubble→dismiss, trending panels, lint UI flows, spend panel) live in `.superpowers/sdd/progress.md` and the task reports under `.superpowers/sdd/` (git-ignored scratch; M10/M12 final-review outputs list them item-by-item). Known UX caveats to expect: trending's newest week is the in-progress week (low count by nature); over-budget LLM calls degrade with a "raise budget" affordance.

## Product concept (the core loop)

Keep SciSpark's core logic, drop the clinical framing:

1. **Home = personalized paper feed** ("news ingestion" style recommendations, learns from user behavior).
2. **Click a paper card → paper-specific digest** (AI summary, lay summary vs. abstract, figure digest, key methods/results breakpoints, related papers).
3. **NEW — "Add to knowledge base" button** on papers/digests. Ingesting a paper triggers LLM-wiki-style generation: automatic metadata retrieval, auto-tagging, and hierarchical organization into the user's personal wiki.
4. **Wiki = LLM-maintained knowledge base** (Karpathy's LLM-wiki pattern): the agent integrates each new paper across existing wiki pages (entities, concepts, methods, syntheses), maintains cross-references/wikilinks, flags contradictions, keeps an index. Knowledge compounds instead of being re-derived per query.
5. **In-app paper reader**: the user reads the full paper inside our app (web or desktop — never bounced out to a third-party viewer), and can highlight passages persistently. Highlights, digests, and wiki pages all support **highlight-to-ask**: select any text and talk to the AI assistant about it — ask questions or ingest the user's own ideas/notes into the knowledge base.
6. **Visualization dashboard** over the wiki's derived data — four views in v1: knowledge graph (like `llm_wiki`'s Sigma.js graph), field chronology/timeline, citation/influence flow, author/team collaboration network.

**Constraints set by the founder (Tong):**
- No third-party knowledge apps (no Obsidian, no Zotero). Browser is the home first; desktop/mobile apps may follow.
- General research audience, not clinicians. Remove clinical copy, medical-specialty taxonomies, and clinical onboarding.
- The feed-first home experience is non-negotiable — it is SciSpark's identity.

## Related repos (read these before designing or reusing code)

| Path | What it is | What to take from it |
|---|---|---|
| `/Users/tongshan/Documents/scispark-app-frontend` | Frontend prototype (Next.js 16 App Router, React 19, Tailwind v4, Zustand 5, framer-motion 12). **All data is mocked; zero real AI/API calls** — chat streaming, reasoning animation, digests are pure UI over `src/lib/mock-data/`. State persists to localStorage. | The whole UI architecture is reusable: AppShell 3-column layout, feed (`src/hooks/useFeed.ts`, `FeedCard`), paper digest page (`src/app/paper/[id]/page.tsx`), chat with citations/sources panel, projects/notes/library, highlight-to-note. Clinical parts are content-only (mock papers, specialty colors, onboarding questions, copy). Its own CLAUDE.md warns Next.js 16 has post-training-data breaking changes — read its `AGENTS.md`/bundled docs before editing. |
| `/Users/tongshan/Documents/scispark-landing` | Marketing site (Next.js 16, Tailwind v4, next-intl en/zh, Supabase waitlist, Vercel). | Brand/design system: warm cream/espresso palette (`#fefaf5` bg, `#2b180a` text, orange accent `#f97316`), Halant serif headings + Geist body, 28px card radii, grain texture. Tokens in its `src/app/globals.css` and `docs/claura-reference-tokens.md`. |
| `/Users/tongshan/Documents/research-os-dev` | Tong's prior Python CLI ("Research OS"): local-first Markdown knowledge layer driven by coding agents. | The **OKF bundle model** (`src/research_os/okf.py`): Markdown + YAML frontmatter, path-as-ID concepts, link → graph-edge derivation, safe agent upserts via managed-section markers, confirmation gates before knowledge writes. Also `graph.py` (bundle → graph.json), `context.py` (query → context packet for agents), and the React Flow visual explorer (`visual-app/`). Zotero/Obsidian coupling is thin and isolated — the core model ports cleanly to a browser app. |
| `/Users/tongshan/Documents/scispark_context` | Pitch deck + StartX application. | Product positioning, long-term vision ("expand beyond medicine to every field of research"), team, business phasing. |

## External references

- **LLM-wiki concept**: https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f — three layers (immutable raw sources → LLM-owned wiki pages → schema/convention doc); ingest/query/lint operations; a single ingested source may update 10–15 wiki pages.
- **llm_wiki implementation**: https://github.com/nashsu/llm_wiki — Tauri + React desktop app; two-step ingest (analyze → generate); four-signal graph relevance (wikilinks ×3.0, shared sources ×4.0, Adamic-Adar ×1.5, type affinity ×1.0); Louvain community coloring; Purpose.md scoping; review panel for human-in-the-loop.
- **SciSpark landing (live)**: https://landing.scispark.ai/

- **Wiki schema (approved 2026-07-11)**: adopt llm_wiki's schema mechanism wholesale — user-editable `schema.md` as authoritative type→directory routing injected into ingest prompts; strict frontmatter contract (`type, title, created, updated, tags[], related[]` as bare slugs, mandatory `sources[]` provenance); wikilinks in body only; `index.md`/`log.md` maintained by the app deterministically, never by the model. **10 types**: `paper` (their `source` + structured IDs: doi/arxiv/openalex/authors/year/venue/`projects[]`), `concept`, `method`, `finding`, `comparison` (all ≈ theirs), `author` (split from their `entity`; OpenAlex ID), `topic` (their `synthesis`), `note` (ours: user ideas), `idea` (ours: Spark Skill cards), `project` (ours: virtual index/context scope). Dropped for v1: `thesis`, `query`. Reserved files: `index.md`, `log.md`, `purpose.md`, `schema.md`. llm_wiki clone for reference is in the session scratchpad; re-clone from https://github.com/nashsu/llm_wiki if needed.

- **Ingest pipeline (approved 2026-07-11)**: acquire full text (HTML/PDF via proxy; paywalled → metadata+abstract+digest with `full_text: false`) → deterministic pre-fill of structured frontmatter from API metadata (code, not LLM: DOI/IDs/authors/venue + author page skeletons) → assemble context (purpose.md, schema.md, index, existing digest reused, user highlights/questions as emphasis signals, target projects) → LLM Step 1 analysis (llm_wiki prompt near-verbatim) → LLM Step 2 generation via **structured output/tool-call** (not delimiter parsing): `{files[], reviews[]}` → validate against schema routing → apply as **one atomic changeset** (all-or-nothing) → app deterministically updates index.md/log.md/project indexes → REVIEW items to queue → derived views incrementally rebuild. Retry-once-with-error; final failure → draft in review queue (expected <1% with frontier models). Review queue is non-blocking, one-click constrained actions, every action itself an undoable changeset.

- **Recommendation = AI-native agentic RecSys (Tong, 2026-07-11)**: keep the traditional funnel (retrieve → rank → re-rank) but every stage is LLM reasoning over assembled user context (onboarding questionnaire, save/like history, per-paper dwell time, chat memories) — no trained ML models, no collaborative filtering (v1), **no third-party embeddings (no SPECTER)**. Stage-1 retrieval is agent-formulated searches over the APIs' native tools (keyword/concept/author/citation-graph); if similarity search ever proves necessary, only a small on-device embedding model as a private tool. Re-rank stage produces per-card personalized "why this, why you, why now" explanations. Public side: a **trending agent** runs daily on SciSpark's end producing per-field trend surveys, cached for all users (fixed cost, user-independent) and feeding stage-1 candidates. Linjing's prior ML semantic recsys is reference, not foundation.
- **Agent behaviors are versioned skill documents** ("Agentic Research Feed Skill" first): the harness executes standardized skill definitions (context → workflow → output contract), mirroring research-os `workflows/*.md` and llm_wiki skills. Layer-4 organizing principle: one harness, N skills (Feed, Trending, Ingest, Lint, Reading-Companion).

- **User model = two-tier memory (approved 2026-07-11)**: Tier 1 append-only raw event log in the vault (`.scispark/events/`: views+dwell, highlights, saves/likes/dismissals, agent chats — local, exported with vault, ground truth, replayable). Tier 2: a Memory-Consolidation Skill periodically distills events into **user-readable/editable wiki pages** — `profile.md` (seeded by onboarding), `interests.md` (evidence-linked), `feedback.md` (standing instructions). Recommender agent receives Tier 2 + recent raw events. Editing the pages IS retraining; transparency is a product feature. Lint watches for summary-vs-log drift.

- **Backend (approved 2026-07-11)**: one deployment — the web app and API are a single Next.js app on Vercel. Server surface: `/api/search/{arxiv|openalex|s2|pubmed}` (normalized passthrough, server-held keys, shared caching), `/api/resolve` (DOI→OA location via Unpaywall), `/api/fetch` (CORS relay for public PDFs/HTML, streaming, size-capped, IP rate-limited), `/api/trending/{field}` (serves daily trending-agent output from blob storage), one daily cron running the Trending Skill on SciSpark's own key (the only server-side LLM spend, fixed per day). No server LLM endpoint for users (BYOK goes client→provider directly). Proxy logs no query strings.
- **Entry flow (Tong, 2026-07-11)**: anonymous visitors land on the **Trending page** (public, zero setup — doubles as live demo). "Personalized home" requires completing the conversational agent-style onboarding, which creates a **local profile only** (no server account — this is the "registration"). At the end of onboarding, **optionally** offer real account registration (email, Supabase) purely for marketing/user-info collection and future tiers — never required, gates nothing in v1. Trending remains a tab for onboarded users and feeds recommender stage-1 candidates.

- **Agent harness (approved 2026-07-11)**: providers in v1 = Anthropic, OpenAI, Google, OpenRouter (all browser-BYOK-capable; Ollama v1.5). Skills declare **model tiers** (`fast`/`strong`) per step, never model names; user settings map tiers→models with sane defaults. Harness meters every skill run (tokens→estimated cost, visible "AI spend" panel) and enforces a **user-set daily budget** with graceful degradation (fewer candidates / cached trending / manual refresh). Budget enforcement is harness-level, not per-skill.

- **Tool registry + safety contract (approved 2026-07-11)**: skills get per-manifest tool allowlists (`vault.read/search/list` all; `vault.propose_changeset` only Ingest/Lint/Memory-Consolidation; `papers.search/citations` Feed/Trending/Reading-Companion; `papers.fetch` Ingest/Reading-Companion; `events.query` Feed/Memory-Consolidation; `user.flag` all). Four harness-enforced rules: (1) no raw writes — only schema-validated atomic undoable changesets; (2) no open network — registered tools only, no arbitrary URL fetch in v1; (3) paper text is untrusted input (prompt-injection defense = rules 1+2, not prompt hygiene alone); (4) mandatory provenance — every changeset logs skill, model, sources.

- **Frontend (approved 2026-07-11)**: fork `scispark-app-frontend` as the starting codebase — keep AppShell/feed/digest/chat/highlight architecture and design system; strip clinical content; replace mock hooks with real services behind the same interfaces. Wiki editor: **Milkdown** (llm_wiki-proven). Graph: **Sigma.js + graphology** (Louvain, Adamic-Adar). Timeline/citation/author views: custom D3 over derived data. Reader: **pdf.js** + quote/position-anchored highlight overlay; native HTML rendering for arXiv/PMC HTML full texts.

- **Research Companion (Tong, 2026-07-11)**: a persistent, cute, proactive AI companion ("Codex-pet-like", **text-only — no voice/audio**) is the product's personality and the presentation layer of the skill system — greets on open, proactively observes while reading ("related to [[x]] — import?"), celebrates ingests, suggests Spark runs on detected clusters. **Every conversational surface (onboarding, KB chat, select-to-ask, review discussions, spark sessions) renders as chat with the one companion persona**; skills are invisible backend; persona is a rendering layer only, kept out of analysis-quality-critical skills. Deterministic free triggers + fast-tier utterances; anti-Clippy contract (proactivity budget, chattiness setting, always dismissible, learns from dismissals via Tier-1 events); proposes but never auto-runs expensive/mutating skills. Companion named **Ember** (Tong, 2026-07-13) — a small warm spark-companion; defined as a single swappable constant in `src/lib/companion/persona.ts`. End-users can also rename their own companion via settings (companionName in .scispark/settings.json). Mascot SVG art still provisional (M11 branding).
- **Spark Skill (Tong, 2026-07-11)** — the "Spark" in SciSpark: vault-grounded research-idea generation adapted from MIT-licensed microsoft/ResearchStudio (ResearchStudio-Idea; clone in scratchpad): ground (warm from vault + fresh API retrieval) → bottleneck diagnosis → pattern-guided ideation (their 15-pattern/31-sub-pattern cards bundled) → two-channel scoop-check (signature + alias terms) → 5-check audit with locked falsification fields + honest do_not_generate → **`idea` wiki page** (10th page type, `wiki/ideas/`, status + depth frontmatter) with mini lit-review. **Two modes (Tong)**: Quick Spark (vault-only, single strong call, cents — 2–3 idea seeds, companion offers freely) and Deep Spark (full pipeline, ~$1–3, always user-confirmed with cost estimate; "develop fully" upgrades a seed). Caveat: pattern cards are ML-conference-mined; field-specific mining is v2.

## Architecture principles agreed so far

- **Wiki layer follows the LLM-wiki three-layer split**: raw sources (papers) are immutable; the wiki is agent-owned and regenerable; a schema document governs page types, taxonomy, and agent behavior.
- **Adopt Research OS's safety pattern**: agent writes to the knowledge layer go through structured proposals/managed sections rather than free-form overwrites; graph and index views are deterministic projections derived from the wiki content, never hand-maintained.
- **The knowledge graph is derived, not stored**: nodes/edges come from wikilinks, shared-source relations, and typed frontmatter relations.

## Decisions (Tong, 2026-07-11)

- **Design is split into 5 layers, worked in this order**: (1) whole product, (2) system, (3) back-end system design, (4) agentic-AI harness / function design, (5) front-end design. Front-end is lowest priority initially — borrow `scispark-app-frontend`; the UI is expected to change once the product solidifies.
- **Storage: local-first markdown (Obsidian-like) for MVP** — but SciSpark owns the markdown renderer via its web app (no Obsidian). Cloud persistence/sync is a future paid tier (v2.0, motivated by mobile apps), not free tier.
- **Paper sources**: arXiv + OpenAlex + Semantic Scholar + PubMed (all-field coverage). If forced to prioritize one domain: biomedical.
- **Real LLM calls from the start** — no mock-first phase for the new system.
- **Accounts**: architecture must support an account system for future price tiers, but the MVP runs fully locally with no sign-up required.
- **Product identity**: parallel exploration alongside the clinical SciSpark app (not an immediate replacement).
- **Borrow llm_wiki's machinery, differentiate on the gap it left**: adopt its two-step ingest (analyze → generate), wiki file conventions, multi-signal graph relevance, and human-review queue. llm_wiki has **no in-app reading of original papers, no highlighting, no select-to-ask, and no discovery feed** (verified 2026-07-11) — those are exactly our additions: discover (feed) → read (in-app reader + highlights) → understand (digest + ask-AI) → retain (agent-maintained wiki). Reading highlights/questions double as personalization signals for what the wiki emphasizes.

- **LLM access: BYOK now, proxy later (phased)**. MVP: user brings their own API key (stored locally, calls go direct to the provider — llm_wiki-style). When accounts/price tiers arrive, paid users get SciSpark-managed keys via a server proxy; BYOK remains the free/private tier. Build one `LLMProvider` interface from day one so the proxy is just a second implementation.

- **Visualizations — all four views are in the vision**, built as pluggable views over the same derived data: knowledge graph (baseline, llm_wiki-style), field timeline/chronicle, citation/influence flow, author/team collaboration network.
- **System architecture: Option 1** — local-first browser app; vault (markdown+PDFs+annotations) behind a `VaultStorage` interface (OPFS default + File System Access API folder on Chromium); agent harness runs client-side; one thin stateless serverless proxy for CORS/PDF-fetch/Unpaywall/feed aggregation (public data only, no accounts, no storage). Tauri wrapper and cloud/paid tier plug in later without rewrites. **SUPERSEDED (Tong, 2026-07-14, M11): "the browser is only for showing UI; runtime is on user."** v1 is a **local app** — a Next.js server on the user's machine owns the runtime (vault = plain files on disk via Node `fs` at `SCISPARK_VAULT`, default `~/SciSpark/vault`; agent harness + BYOK keys server-side; browser = UI over a vault API + skills API, any browser, no OPFS/FSA). Tauri wrapper and hosted tier remain the post-v1 layers. See `docs/superpowers/specs/2026-07-14-m11-local-runtime-design.md`.
- **v1 scope (Tong, 2026-07-11)**: onboarding-lite interest profile; personalized feed; LLM-generated digest; in-app reader (HTML/pdf.js) with highlights + select-to-ask; add-to-KB → two-step agent ingest → wiki; wiki browse/edit; KB chat with citations; **visualization dashboard with all four views** (graph, timeline, citation flow, author network); **projects = virtual indexes, not physical folders**: a project is an index page (links to papers/notes/concepts + optional instructions) whose sole role is scoping agent context when chatting/working "in" that project; UI may render it as a folder tree, but the vault's physical layout stays fully agent-owned; **wiki lint**; slim async review queue (agent writes immediately; per-ingest changesets with one-click revert; LLM-flagged uncertainties + lint findings share one non-blocking inbox — llm_wiki's model plus undo, which llm_wiki lacks). Vault zip export; **Research Companion**; **Spark Skill**. OUT of v1: Tauri wrapper, accounts/cloud sync, proxy-LLM tier, mobile.

- **M10 reframed from public trending to personalized trending dashboard (Tong, 2026-07-14)**: the original roadmap M10 ("public/anonymous trending page + server-side Vercel cron writing Vercel Blob + `/api/register`/Supabase marketing capture") is dropped for v1. M10 is instead a **personalized field-trends dashboard**: fixed sub-fields seeded from `interests.md` (editable on `/profile`), deterministic quantitative metrics/charts (paper counts, %Δ, weekly volume, top movers/venues — never LLM-emitted) plus a persona-free qualitative LLM survey (notable-paper whys, emerging topics, momentum narrative), all client-side/BYOK. A **staleness-triggered refresh** (cadence setting + cached `generatedAt` + manual Refresh) stands in for a v1 "cron," since a browser can't run a schedule while closed and there's no backend to hold a per-user key. The public/cron/Blob/register model remains a documented **v2** growth path once a real backend + accounts exist. See `docs/superpowers/specs/2026-07-14-m10-trending-dashboard-design.md`.

## Open questions (resolve with Tong before building)

- Mechanism for local-first storage in a browser (File System Access API real folder vs. OPFS vs. Tauri-first) — being settled in the architecture-approaches proposal.
